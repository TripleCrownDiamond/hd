/**
 * The shop name, in one place.
 *
 * Admin settings can override the name shown in the footer and on invoices
 * (`site_settings.company_name`); this is the fallback every surface uses until
 * that row is filled in, and the name baked into metadata and e-mail templates.
 */
export const BRAND_NAME = "HolzDirekt";

/** Used where the wordmark is spelled out for screen readers or `alt` text. */
export const BRAND_TAGLINE = "Brennholz und Kaminöfen online bestellen";
