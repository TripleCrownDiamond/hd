# Dependencies and builds
node_modules/
.next/
out/
coverage/
playwright-report/
test-results/

# Local environment and secrets
.env
.env.local
.env.*.local
!.env.example

# Hosting and tools
.vercel/
.turbo/
.cache/

# Supabase local runtime
supabase/.branches/
supabase/.temp/

# Editors and OS
.idea/
.vscode/*
!.vscode/extensions.json
.DS_Store
Thumbs.db

# Logs
*.log
pnpm-debug.log*

# Scraping outputs (raw data — never commit; requires human review before publish)
/data/scraped/

# Temp / corrupt dirs
node_modules.corrupt*/
tmp/
*.log
.dev-server*.log
.next-dev*.log
.next-stale*/

# TypeScript incremental build info
tsconfig.tsbuildinfo
# Backups d'environnement. `.env.local` est ignore, mais une copie
# `.env.local.bak` ne l'etait pas et un `git add -A` l'aurait committee
# avec la cle service_role dedans.
*.bak
.env*.backup

# Local artefacts (screenshots, ad-hoc scripts)
output/playwright/
scratchpad/

# Isolated Next.js dev builds (run doc: NEXT_DIST_DIR per port)
.next-*/
