# Public application
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Cloudinary (image delivery + upload script)
# CLOUD_NAME is public and hard-coded in src/lib/cloudinary.ts; API key/secret
# are ONLY used server-side by scripts/upload-image.mjs — never exposed to the client.
CLOUDINARY_CLOUD_NAME=pq4soawt
CLOUDINARY_API_KEY=159766499532764
CLOUDINARY_API_SECRET=

# ─────────────────────────────────────────────
# Supabase
# ─────────────────────────────────────────────
# Local dev via `supabase start`:
#   NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
#   NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=<anon key printed by `supabase start`>
#   SUPABASE_SECRET_KEY=<service_role key printed by `supabase start`>  # PRIVILEGED, server-only
#
# Hosted project:
#   NEXT_PUBLIC_SUPABASE_URL=https://<ref>.supabase.co
#   NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=<anon key from Project Settings → API>
#   SUPABASE_SECRET_KEY=<service_role key — Project Settings → API>
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=

# Kept for backwards compat with older Supabase docs (same value as SUPABASE_SECRET_KEY)
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# Supabase server-only
SUPABASE_SECRET_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_STORAGE_BUCKET_PRODUCTS=products
SUPABASE_STORAGE_BUCKET_DOCUMENTS=documents
SUPABASE_STORAGE_BUCKET_INVOICES=invoices

# Commerce
INVOICE_NUMBER_PREFIX=RE
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=

# Transactional messaging
#
# SMTP is preferred and takes precedence: the shop sends from its own domain
# mailbox. Hostinger uses smtp.hostinger.com, port 465 (implicit TLS) or 587
# (STARTTLS). SMTP_USER must be the full mailbox address, and SMTP_FROM_EMAIL
# must match it — Hostinger rejects a From it did not authenticate.
#
# SMTP_PASSWORD is the mailbox password. Never commit it: set it in .env.local
# locally and in the host's environment panel in production.
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=noreply@holzdirekt.store
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@holzdirekt.store
SMTP_REPLY_TO=

# Fallback transport, used only when no SMTP credentials are present.
RESEND_API_KEY=
RESEND_FROM_EMAIL=
RESEND_REPLY_TO=
RESEND_ADMIN_EMAIL=
TELEGRAM_BOT_TOKEN=
TELEGRAM_ADMIN_CHAT_ID=

# AI assistant
AI_PROVIDER=
AI_API_KEY=
AI_MODEL=
MISTRAL_API_KEY=
MISTRAL_CHAT_MODEL=mistral-small-latest

# Operations
SENTRY_DSN=
ADMIN_EMAIL=
CRON_SECRET=
CART_RECOVERY_SECRET=
ORDER_TRACKING_SIGNING_SECRET=

# ImageKit — media provider for new uploads (Cloudinary holds the older assets).
# Dashboard → Developer options. The private key is server-side only.
NEXT_PUBLIC_IMAGEKIT_ID=
NEXT_PUBLIC_IMAGEKIT_URL=
IMAGEKIT_PUBLIC_KEY=
IMAGEKIT_PRIVATE_KEY=
